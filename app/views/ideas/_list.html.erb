<div class="row">
    <div class="=col-2">
    </div>

    <div class="col-10">
    <% @ideas.each do |idea| %>
        <div>
            <h3><%= link_to idea.title, idea_path(idea) %></h3>
            <h3><%= idea.description %></h3>   
        </div>
    <% end %>
    </div>
</div>